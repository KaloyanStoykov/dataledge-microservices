pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('datasourceService'){
                    sh './mvnw -B -DskipTests clean package'
                }
            }
        }
    }
}

